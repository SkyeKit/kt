# 使用国内镜像源加速依赖安装（npmmirror）
registry=https://registry.npmmirror.com
# pnpm 内容寻址缓存（store）固定到用户目录，避免沙箱拦截默认盘符目录
store-dir=C:/Users/13850/.workbuddy/pnpm-store
# pnpm 严格校验 store 完整性时跳过（镜像源哈希偶发不一致）
strict-store-purity=false
